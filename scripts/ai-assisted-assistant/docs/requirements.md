Write a simple webpage that will:
1. Authenticate me in JIRA available from https://scylladb.atlassian.net/jira.
2. Get all issues assigned to me that:
  * were modified during the last two weeks
  * are in state different than New
3. Present the list of PRs and the checkbox next to them. The view should present basic information about PRs in dense view.
4. After I select some of those PRs using checkboxes and press "I am feeling lucky" button it will add "ai-assisted" label to all of those selected PRs. 
